numero = 10
otro_numero = 20 

print(id(numero))
print(id(otro_numero))

#id retorna en que parte de la memoria guarda Python ese valor, por cada vez que se ejecuta el programa cambia su numero. Si las variables tiene el mismo valor se guardan en el mismo espacio de memoria. Esto pasa con los tipos de datos inmutables unicamente 
